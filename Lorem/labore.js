   // Assume `key` is a variable that holds the key of the cell you want to access
   let key = 'cell2';
   
   // Accessing the tuple property
   let t = cells[key].tuple;
   
   console.log(t); // Output: [4, 5, 6]
   